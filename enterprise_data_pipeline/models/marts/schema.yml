version: 2

models:
  - name: fct_delivery_analysis
    description: "配送コスト分析用の最終ファクトテーブル。Pure SQLで計算した配送コストを含む。"
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
      - name: delivery_cost
        description: "計算された配送コスト（円）。"
        tests:
          - not_null
